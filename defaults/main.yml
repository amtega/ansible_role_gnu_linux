---
# Role default lower priority variables

# Internet proxy

#gnu_linux_http_proxy:
#gnu_linux_https_proxy:

# List with domains/IPs to bypass previous proxy settings
#gnu_linux_no_proxy:

# Number of seconds to delay the reboot handler

gnu_linux_reboot_delay: 5

# Number of seconds to delay the wait handler

gnu_linux_wait_delay: 20

# Number of seconds to sleep between wait attemps

gnu_linux_wait_sleep: 3

# Timeout for wait handler

gnu_linux_wait_timeout: 300

# Time for pause handler

gnu_linux_pause_time: 5
